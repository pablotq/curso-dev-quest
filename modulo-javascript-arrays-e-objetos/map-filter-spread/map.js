// map - cria um novo array com base num array original

const numbers = [1, 2, 3, 4, 5];

// const dobroNums = numbers.map((number) => {
//   return number * 2;
// });

// versão curta 
const dobroNums = numbers.map(number =>number * 2);


// console.log(numbers);
// console.log(dobroNums);

